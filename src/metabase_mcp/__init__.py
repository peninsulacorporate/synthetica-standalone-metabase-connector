"""metabase-mcp-server — dual-mode Metabase MCP server.

The public API is the :mod:`metabase_mcp.server` entrypoint; most callers
run the CLI ``metabase-mcp`` and do not import from this package.
"""

__version__ = "0.1.0"
